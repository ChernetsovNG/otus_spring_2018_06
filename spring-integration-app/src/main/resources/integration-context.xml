<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/integration
        http://www.springframework.org/schema/integration/spring-integration-5.0.xsd">

    <int:gateway service-interface="ru.nchernetsov.integration.PrinterGateway"
                 default-request-channel="inputChannel"/>

    <int:channel id="inputChannel"/>
    <int:channel id="outputChannel"/>
    <int:channel id="enricherChannel"/>
    <int:channel id="requestChannel"/>

    <bean id="customService" class="ru.nchernetsov.integration.CustomService"/>

    <int:service-activator ref="customService" input-channel="requestChannel" method="getPhoneNumber"/>

    <int:enricher input-channel="inputChannel" request-channel="requestChannel" output-channel="enricherChannel">
        <int:property name="phoneNumber" expression="payload"/>
    </int:enricher>

    <int:object-to-json-transformer input-channel="enricherChannel" output-channel="outputChannel"/>

    <bean class="ru.nchernetsov.integration.PrintService" id="printService"/>

    <int:service-activator ref="printService" method="print" input-channel="outputChannel"/>

</beans>
